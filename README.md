This is a bot client using [Pyrogram](https://docs.pyrogram.org/)

# Prepare
Run `cp .env.example .env` or `make env`   
Set `API_ID`, `API_HASH` and `BOT_API_TOKEN` in `.env`
# Run

```bash
make build
make run
```
Bot running in the background